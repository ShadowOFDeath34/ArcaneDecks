export default function Home() {
  return (
    <main style={{ padding: 40, fontFamily: "system-ui" }}>
      <h1>Arcane Decks: Goblin Siege — Backend</h1>
      <p>API routes: /api/v1/auth/anonymous, /api/v1/progress, /api/v1/leaderboards/:id, /api/v1/iap/webhook</p>
    </main>
  );
}
